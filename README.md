# Libris
A system that converts directories into shareable vector DB memory. People can use these shared VDB to get quick answers about their files stored in sharepoint or drives or local directories. 
